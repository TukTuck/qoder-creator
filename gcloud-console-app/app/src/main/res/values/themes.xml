<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Kein AppCompat/Material-Theme noetig: die komplette UI ist Compose.
      Ein schlankes Platform-Theme reicht und spart eine Dependency.
    -->
    <style name="Theme.ConsolePocket" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
